package agent

// Docker-based execution environments are planned as a future feature.
// When implemented, the agent will run inside a dev container that provides
// the project's full toolchain (Go, Node, Python, etc.) with the repo mounted.
// This is intentionally not implemented yet — the design needs more thought.
